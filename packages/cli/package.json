{
  "name": "@mugi111/flowui-skills",
  "version": "0.1.0",
  "description": "FlowUI Skills command-line interface.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "flowui": "./dist/index.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "rm -rf dist && npm run build && node scripts/run-tests.mjs"
  },
  "dependencies": {
    "playwright": "^1.63.0",
    "yaml": "^2.9.1"
  }
}
