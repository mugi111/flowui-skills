import assert from"node:assert/strict";import test from"node:test";import{renderPlaywright}from"./playwright.js";test("renders a gate-backed test without credentials",()=>assert.doesNotMatch(renderPlaywright({schema_version:"1.0",id:"x",status:"draft",intent:"x",start_page:"p",steps:[]}),/do-not-leak/));
