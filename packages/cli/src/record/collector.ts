import { redactValue, type RedactionPolicy } from "../shared/redaction.js";
export interface RecordedEvent { readonly sequence:number; readonly documentId:string; readonly type:"click"|"input"|"navigation"; readonly target:string; readonly value?:unknown; }
export class RecordCollector { private events:RecordedEvent[]=[]; private stopped=false; constructor(private readonly policy:RedactionPolicy={}) {} add(event:Omit<RecordedEvent,"sequence">):void { if(this.stopped) throw new Error("recording is stopped"); this.events.push({...event,sequence:this.events.length+1,...(event.value===undefined?{}:{value:redactValue({value:event.value},{...this.policy,sensitiveKeys:[...(this.policy.sensitiveKeys??[]),"value"]})})}); } stop():readonly RecordedEvent[]{this.stopped=true;return this.events;} }
