import { redactValue, type RedactionPolicy } from "../shared/redaction.js";
export interface RecordedEvent { readonly sequence:number; readonly documentId:string; readonly type:"click"|"input"|"navigation"; readonly target:string; readonly value?:unknown; }
export class RecordCollector { private events:RecordedEvent[]=[]; private stopped=false; constructor(private readonly policy:RedactionPolicy={}) {} add(event:Omit<RecordedEvent,"sequence">):void { if(this.stopped) throw new Error("recording is stopped"); const sensitive=/password|passphrase|secret|token/i.test(event.target)||this.policy.sensitiveKeys?.some(key=>key.toLowerCase()===event.target.toLowerCase()); this.events.push({...event,sequence:this.events.length+1,...(event.value===undefined?{}:{value:sensitive?redactValue({value:event.value},{sensitiveKeys:["value"]}):event.value})}); } stop():readonly RecordedEvent[]{this.stopped=true;return this.events;} }
