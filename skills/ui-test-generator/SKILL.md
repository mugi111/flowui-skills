# ui-test-generator

## 概要
確定ScenarioからPlaywright Testを生成します。許可やsecretをコードへ埋め込みません。

## Purpose
Render gate-backed Playwright tests from ready Scenarios.

## Procedure
Validate first, then use `flowui render playwright`.

## Rules
Do not invent assertions or bypass the Safety Gate.
